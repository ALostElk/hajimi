# 两人合作的立项报告：哈基米计算器

## 1.组名

***凯迪集团***

---

## 2.组员

***王岩方 李凯迪***

---



## 3.软件功能

### **基础功能**

#### 1 界面结构

* **显示区** ：
* 展示用户输入与计算结果。
* 支持多行输入与实时更新。
* 当输入错误时提示“输入错误”。
* **按键区** ：
* 分为数字键、运算符键、科学计算功能键、控制键。

---

#### 2 数字与基础运算

* **数字键** ：0–9
* **小数点 `.`** ：支持浮点数输入。
* **四则运算** ：`+`、`-`、`×`、`÷`
* **百分号 `%`** ：支持百分比运算（如 `50%` = 0.5）。

---

#### 3 高级科学功能

* **幂运算 `xʸ`** ：任意次方。
* **平方根 `√x`** ：返回正平方根。
* **阶乘 `x!`** ：整数输入时返回阶乘值，负数或小数提示错误。
* **倒数 `1/x`** ：返回输入的倒数，0时报错。

---

#### 4 三角与反三角函数

* **三角函数** ：`sin`、`cos`、`tan`
* 输入为角度（默认单位：度）。
* 可切换 `deg / rad`（度数 / 弧度）。
* **反三角函数** ：`sin⁻¹`、`cos⁻¹`、`tan⁻¹`
* 输出以当前角度单位表示。

---

#### 5 对数与指数函数

* **自然对数 `ln(x)`** ：以 e 为底。
* **常用对数 `lg(x)`** ：以 10 为底。
* **指数 `eˣ`** ：返回 e 的幂。

---

#### 6 常数支持

* **π (圆周率)** ：3.1415926…
* **e (自然常数)** ：2.7182818…

---

#### 7 控制功能

* **AC（All Clear）** ：清空所有输入与结果。
* **退格键 ⌫** ：删除最后一个输入字符。
* **括号 `(` `)`** ：支持复合运算与优先级调整。
* **模式切换键 `2nd`** ：扩展功能键（例如二级函数：平方、反三角等）。
* **简写切换键：** 仅保留基础运算模式(加减乘除)

---

#### 8 输入与输出规则

* **合法性检测** ：对非法输入（如 `√-1`、`ln(-5)`、除以零等）给出提示而不是报错。
* **多步运算** ：遵循数学运算优先级（括号 > 幂运算 > 乘除 > 加减）。
* **显示格式** ：
* 普通数值保留 8 位小数。
* 超大/超小数值自动转换为科学计数法。

---



### 哈基米适配

#### 音效适配

* **按键音效** ：每次点击按键播放网络流行“哈基米”相关音效。
* **特殊运算音效** ：大数结果、阶乘结果、sin/cos 特殊值时触发特殊音效。
* **静音键设置**：为使用者提供静音功能，点击后切换为静音计算器

---



#### 输出框：动画匹配

* **动态语句** ：
* 普通结果（结合AI简单更改） → “哈基米：结果是 xxx”
* 特殊情况(音乐) →
  * 结果为 0 → “哈基米：曼波！”
  * 结果为负数 → “哈基米：波曼！”
  * 巨大结果 → “哈基米：哈基米！哈！”
  * bug→ “哈基米：阿米诺斯！你脑子呢？”
* **输出动画** ：结果显示时可抖动、闪光或加 emoji。

---



#### 彩蛋互动

* 输入特定数字或公式触发彩蛋：
  * 输入 `520` →
  * 输入 `2333` →
  * 输入 `π` 并按等号 →
  * 输入 `114514` 并按等号 →

---



### 健康扩展功能（BMR 计算）

* **输入** ：性别、年龄、身高、体重。
* **输出** ：基础代谢率（kcal/天）。
* **趣味语（结合AI）** ：
* 代谢低 → “哈基米：要多注意饮食哦～”
* 代谢中等 → “哈基米：刚刚好，健康平衡！”
* 代谢高 → “哈基米：曼波！BMR=1500，你的身体在躺平时候都这么努力”
* **可选扩展** ：根据活动水平计算 TDEE（日常总消耗）。

---

## 4.适配对象：
* **需要简单数字计算的使用者**
* **需要计算基础代谢率的使用者**

---

## 5.技术路线与进度安排

### 技术架构
```
哈基米计算器
├── 前端层 (UI Layer)
│   ├── 主界面 (HajimiUI) - tkinter
│   ├── 显示区 - 多行文本显示
│   └── 按键区 - 响应式按钮布局
├── 业务逻辑层 (Business Layer)
│   ├── 计算引擎 (Calculator) - 数学运算核心
│   ├── AI反馈 (HajimiAI) - 智能结果反馈
│   └── 角色系统 (HajimiCharacter) - 音效与互动
└── 数据层 (Data Layer)
    ├── 音效素材 (shucai/) - 哈基米音效库
    └── 配置文件 - 用户设置
```

### 开发阶段规划

#### 第一阶段：核心功能完善 (已完成基础框架)
**时间：当前状态**
- ✅ 基础UI框架搭建
- ✅ 核心计算引擎实现
- ✅ 哈基米AI反馈系统
- ✅ 音效系统集成
- ✅ BMR计算功能

#### 第二阶段：功能增强与优化 (10.15前)
**优先级：高**
- 🔄 完善科学计算功能
  - 实现幂运算 (xʸ)
  - 添加反三角函数 (sin⁻¹, cos⁻¹, tan⁻¹)
  - 完善阶乘计算 (x!)
  - 添加倒数功能 (1/x)
- 🔄 增强输入验证
  - 非法输入检测与提示
  - 括号优先级处理
  - 多步运算支持
- 🔄 界面优化
  - 科学计数法显示
  - 结果格式化 (8位小数)
  - 动态语句与动画效果

#### 第三阶段：哈基米特色功能 (10.17前)
**优先级：中**
- 🔄 彩蛋互动系统
  - 特殊数字触发 (520, 2333, 114514)
  - π和e常数特殊处理
  - 随机趣味语句
- 🔄 音效系统增强
  - 按键音效匹配
  - 特殊运算音效
  - 静音功能完善
- 🔄 模式切换
  - 简写模式 (仅基础运算)
  - 科学模式 (完整功能)
  - 角度单位切换 (deg/rad)

#### 第四阶段：健康功能扩展 (10.20前)
**优先级：中**
- 🔄 BMR功能完善
  - TDEE计算 (总日常消耗)
  - 活动水平评估
  - 个性化建议
- 🔄 健康数据可视化
  - 代谢率图表
  - 历史记录

#### 第五阶段：测试与优化 (10.25前)
**优先级：高**
- 🔄 全面功能测试
- 🔄 性能优化
- 🔄 用户体验改进
- 🔄 错误处理完善

### 技术实现细节

#### 核心算法优化
```python
# 数学表达式解析增强
def safe_eval(expr):
    # 自定义函数映射
    # 安全性检查
    # 错误处理机制
```

#### 音效系统架构
```python
# 智能音效匹配
def get_sound_by_context(result, operation):
    # 根据计算结果选择音效
    # 支持随机播放
    # 静音模式切换
```

#### AI反馈系统
```python
# 动态语句生成
def generate_response(result, context):
    # 基于结果类型的智能回复
    # 彩蛋触发检测
    # 个性化表达
```

### 开发工具与环境
- **IDE**: VSCode/PyCharm
- **版本控制**: Git
- **依赖管理**: requirements.txt
- **测试框架**: unittest/pytest
- **打包工具**: PyInstaller

### 质量保证
- 单元测试覆盖率 > 80%
- 用户界面测试
- 性能基准测试
- 跨平台兼容性测试

### 风险控制
- **技术风险**: 复杂数学表达式解析 → 使用安全的eval替代方案
- **时间风险**: 功能过于复杂 → 优先级排序，核心功能优先
- **用户体验风险**: 音效过多影响使用 → 提供静音选项
