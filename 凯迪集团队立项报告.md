# 两人合作的立项报告：哈基米计算器

## 1.组名

***凯迪集团***

---

## 2.组员

***王岩方 李凯迪***

---



## 3.软件功能

### **基础功能**

#### 1 界面结构

* **显示区** ：
* 展示用户输入与计算结果。
* 支持多行输入与实时更新。
* 当输入错误时提示“输入错误”。
* **按键区** ：
* 分为数字键、运算符键、科学计算功能键、控制键。

---

#### 2 数字与基础运算

* **数字键** ：0–9
* **小数点 `.`** ：支持浮点数输入。
* **四则运算** ：`+`、`-`、`×`、`÷`
* **百分号 `%`** ：支持百分比运算（如 `50%` = 0.5）。

---

#### 3 高级科学功能

* **幂运算 `xʸ`** ：任意次方。
* **平方根 `√x`** ：返回正平方根。
* **阶乘 `x!`** ：整数输入时返回阶乘值，负数或小数提示错误。
* **倒数 `1/x`** ：返回输入的倒数，0时报错。

---

#### 4 三角与反三角函数

* **三角函数** ：`sin`、`cos`、`tan`
* 输入为角度（默认单位：度）。
* 可切换 `deg / rad`（度数 / 弧度）。
* **反三角函数** ：`sin⁻¹`、`cos⁻¹`、`tan⁻¹`
* 输出以当前角度单位表示。

---

#### 5 对数与指数函数

* **自然对数 `ln(x)`** ：以 e 为底。
* **常用对数 `lg(x)`** ：以 10 为底。
* **指数 `eˣ`** ：返回 e 的幂。

---

#### 6 常数支持

* **π (圆周率)** ：3.1415926…
* **e (自然常数)** ：2.7182818…

---

#### 7 控制功能

* **AC（All Clear）** ：清空所有输入与结果。
* **退格键 ⌫** ：删除最后一个输入字符。
* **括号 `(` `)`** ：支持复合运算与优先级调整。
* **模式切换键 `2nd`** ：扩展功能键（例如二级函数：平方、反三角等）。
* **简写切换键：** 仅保留基础运算模式(加减乘除)

---

#### 8 输入与输出规则

* **合法性检测** ：对非法输入（如 `√-1`、`ln(-5)`、除以零等）给出提示而不是报错。
* **多步运算** ：遵循数学运算优先级（括号 > 幂运算 > 乘除 > 加减）。
* **显示格式** ：
* 普通数值保留 8 位小数。
* 超大/超小数值自动转换为科学计数法。

---



### 哈基米适配

#### 音效适配

* **按键音效** ：每次点击按键播放网络流行“哈基米”相关音效。
* **特殊运算音效** ：大数结果、阶乘结果、sin/cos 特殊值时触发特殊音效。
* **静音键设置**：为使用者提供静音功能，点击后切换为静音计算器

---



#### 输出框：动画匹配

* **动态语句** ：
* 普通结果（结合AI简单更改） → “哈基米：结果是 xxx”
* 特殊情况(音乐) →
  * 结果为 0 → “哈基米：曼波！”
  * 结果为负数 → “哈基米：波曼！”
  * 巨大结果 → “哈基米：哈基米！哈！”
  * bug→ “哈基米：阿米诺斯！你脑子呢？”
* **输出动画** ：结果显示时可抖动、闪光或加 emoji。

---



#### 彩蛋互动

* 输入特定数字或公式触发彩蛋：
  * 输入 `520` →
  * 输入 `2333` →
  * 输入 `π` 并按等号 →
  * 输入 `114514` 并按等号 →

---



### 健康扩展功能（BMR 计算）

* **输入** ：性别、年龄、身高、体重。
* **输出** ：基础代谢率（kcal/天）。
* **趣味语（结合AI）** ：
* 代谢低 → “哈基米：要多注意饮食哦～”
* 代谢中等 → “哈基米：刚刚好，健康平衡！”
* 代谢高 → “哈基米：曼波！BMR=1500，你的身体在躺平时候都这么努力”
* **可选扩展** ：根据活动水平计算 TDEE（日常总消耗）。

---

## 4.适配对象：
* **需要简单数字计算的使用者**
* **需要计算基础代谢率的使用者**

---

## 5.进度安排

* 基础功能：10.10前，结合网络已有开源项目进行修改
* 哈基米素材：10.12前，日常积累

* 哈基米相关音效与显示框适配：10.15
* BMR计算功能与相关拓展：10.17

* 留有三（或十）天冗余
